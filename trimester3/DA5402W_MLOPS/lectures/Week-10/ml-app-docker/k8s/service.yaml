apiVersion: v1
kind: Service
metadata:
  name: ml-app
  namespace: ml-app
  labels:
    app.kubernetes.io/name: ml-app
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ml-app
    app.kubernetes.io/component: api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
